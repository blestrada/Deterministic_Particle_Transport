{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run IMC Crooked Pipe",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/python/src/imc.py",
            "args": [
                "-i", "${workspaceFolder}/python/inputs/crooked_pipe.txt",
                "-o", "${workspaceFolder}/python/inputs/crooked_pipe.out.txt"
            ],
            "console": "integratedTerminal"
        }
    ]
}
